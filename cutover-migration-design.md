# 割接迁移核查系统设计文档（Cutover Migration Check）

> 版本：v1（2026-08-06）
> 定位：替代 `semantic-diff-design-v1/v2` 的"语义割接 Diff"方案。基于**真实割接配置样本**（宁夏 AR 替换割接：`割接前.log` 739KB / `割接后.log` 1064KB，华为 NE5000E 系列）重新设计，从"LLM 语义分析"转向"确定性迁移核查为主、LLM 兜底"。
> 场景：整机替换割接——老设备配置原样搬迁到新设备，端口（槽位/端口号）调整，新设备新增板卡与端口；核心目标是**替换后协议状态全部正常、端口配置完整迁移**。

---

## 1. 问题本质

本系统面向的割接是**设备替换**，不是普通配置变更。特征：

- 老设备每个端口 = 一个配置单元（IP、描述、VRF、MTU、ISIS、策略、成员关系……）；
- 新设备端口 = 同样的配置单元，仅端口名/槽位不同，**内容应原样搬迁**；
- 新设备新增板卡/端口（after 接口数翻倍），新增属于合法变更；
- 核心诉求：验证"每个老端口的内容是否完整搬到对应新端口 + 协议状态是否全部正常"。

因此，**问题的核心不是"哪些有变化"（整机替换变化海量、一眼可见），而是"迁移是否完整且正确"**。这决定了主引擎必须是确定性对比，LLM 只做兜底。

---

## 2. 真实配置样本分析（设计的事实依据）

以下数据来自对 `割接前.log` / `割接后.log` 的本地纯函数分析（未外发）。

| 指标 | 割接前 | 割接后 | 结论 |
|---|---|---|---|
| 接口总数 | 108 | 216 | 新设备接口翻倍（新增板卡/端口） |
| 有 IP 接口（三层口） | 54 | 54 | 全部可 IP 配对 |
| 无 IP 接口 | 53 | 162 | 需描述/内容匹配，其余为新增 |
| IP 匹配迁移对 | — | 54 | 含 Eth-Trunk，主三层口全覆盖 |
| 描述匹配迁移对 | — | 13 | 描述格式"对端设备-对端端口"，搬迁后保留，可靠 |
| 剩余未匹配老端口 | — | 40 | 多为无描述空端口（1 行配置），疑似未用/成员口 |
| BGP 邻居 Total | 6 | 6 | 数量对齐，好信号 |
| 解析器未识别行 | 725 | 734 | 200 上限截断漏掉约 72% |

**逐端口对比抽查结论**（IP 匹配对）：
- Eth-Trunk5 / Eth-Trunk7：两边配置完全一致（0 丢 0 增）→ 验证"端口下配置不变"假设成立；
- Eth-Trunk6：存在真实差异——`mtu 4470 → 9500`、`ipv6 mtu 4470 → 9500` → 逐端口对比能抓真实配置漂移。

**解析器漏网行类型分布**（两套几乎一致）：`ipv6`(210)、`mpls`(176)、`ip`(104)、`carrier`(64)、`qppb-policy`(52)、`trust`(51)、`set`(24-33)、`transmission-alarm`(22)。均为"整类命令未覆盖"，且与现有表格字段基本无关 → **反哺解析器的可学习率约 7-8%，价值有限，不作为设计核心**。

**引用核查的教训**：初次用简单正则检测"被引用但无定义"对象，得到 106/75 个"疑似悬空"，复核发现绝大多数是**定义格式未覆盖导致的误报**（`acl name`、`acl ipv6 name`、`ip community-filter`、`ip ip-prefix` 等格式多样）。结论：引用完整性核查必须**先覆盖全部定义语法**，否则误报率极高、结论不可信。

---

## 3. 设计原则（红线）

1. **确定性引擎为主**：端口映射、逐项对比、协议配对、引用核查全部优先用确定性逻辑；LLM 不得承担"数变化"类工作。
2. **LLM 只兜底**：模糊匹配（无 IP 无描述端口）、引用语义判断、差异风险解释、验证清单生成。
3. **人工确认关键结论**：疑似漏迁、疑似悬空引用、高风险差异必须人工复核后生效，系统不自动下结论。
4. **默认脱敏**：真实配置含公网 IP 与敏感信息，任何外发（LLM 调用）前必须脱敏；纯本地确定性分析不涉及外发。
5. **失败降级**：LLM 不可用不影响确定性核查主体，只折叠语义/兜底部分。
6. **报告可溯源**：每条结论必须引用具体接口、配置行或 diff 字段。

---

## 4. 架构总览：配置分层核查

### 4.0 配置分层核查矩阵

端口只是配置的一半。整机替换的完整核查按"配置层"展开，每层同一套思路：**老设备有什么 → 新设备有没有、对不对**。

| 配置层 | 典型内容 | 解析器认识 | 核查方式 |
|---|---|---|---|
| 端口配置 | IP、描述、VRF、MTU、ISIS、策略引用 | 是 | L2 逐端口对比 |
| 全局系统配置 | sysname、时区、NTP、SNMP、SSH、DNS、日志 | 是 | parseGlobalConfig 逐项对比 |
| 全局对象库 | route-policy、ACL、ip-prefix、community-filter、VPN 实例、VLAN | 部分 | 对象级逐项对比（定义存在 + 内容一致） |
| 协议配置 | BGP 邻居/AS/认证、ISIS 进程、OSPF 区域、LDP、SRv6 locator | 部分 | L3 邻居配对 + 协议参数对比 |
| 静态/默认路由 | ip route-static | 是 | routingStat 对比 |
| 未识别命令 | qppb-policy、carrier、trust、mpls 等（本样本 725/734 条） | 否 | 文本行指纹对比兜底 |

四层技术架构（L1-L4）是实现上述矩阵的方式：L2 覆盖"端口 + 全局系统 + 全局对象 + 未识别命令指纹"四类资产，L3 覆盖协议配置，L4 覆盖跨层引用完整性。

```
  beforeText ─┐
              ├─► 配置块提取（按接口/协议/对象分块）──► L1 端口映射（IP → 描述 → 内容相似度，带置信度）
  afterText  ─┘                                          │
                                                         ▼
                                        L2 资产逐项对比（端口 / 全局系统 / 全局对象 / 未识别命令指纹）
                                                         │  ──► 迁移报告（四类资产）
                                                         ▼
  before/after ──► 协议邻居配对（BGP/ISIS 按 IP）──► L3 协议核查 + 状态验证（自动 SSH）
                                                         │
  afterText ──► 全局对象定义库（全格式） ──► L4 引用完整性核查（确定性初筛 + LLM 兜底）
```

### L1 端口映射（确定性，带置信度）

匹配优先级：
1. **IP 地址相同**（置信度：高）——本样本 54 对；
2. **描述相同**（置信度：中）——本样本 13 对，描述含对端设备-对端端口，搬迁后保留；
3. **配置内容相似**（置信度：中低）——成员口、邻居关系等联合特征；
4. **LLM 辅助模糊匹配**（置信度：低）——前三层未覆盖且非空端口，人工确认后才纳入迁移对。

输出：迁移对列表（含置信度与匹配依据）、未匹配老端口清单（"疑似未用/成员口"或"疑似漏迁"）、未匹配新端口清单（默认归为"新增端口/板卡扩展"）。

### L2 资产逐项对比（确定性，核心）

资产单位不限于端口，同一套"提取 → 规范化 → 逐项对比"逻辑适用于四类：

1. **端口配置**：对每个迁移对（L1 输出），对比两侧接口块全部配置行，输出完全一致 / 有差异（丢/改/增，按影响分级：丢 IP / 丢策略引用 > MTU / 速率 > 描述）/ 空端口。本样本验收基准：Eth-Trunk6 应检出 `mtu 4470→9500`、`ipv6 mtu 4470→9500`；
2. **全局系统配置**：`parseGlobalConfig` 逐项对比（系统名称、时区、NTP、SNMP、SSH、Telnet、DNS、日志、VLAN/路由统计）；
3. **全局对象库**：以对象为资产单位（route-policy / ACL / ip-prefix / community-filter / VPN 实例 / VLAN），老设备每个对象 → 新设备同名对象，逐条对比定义内容（节点、匹配条件、动作）。本样本对象定义 58+ 个、引用 132 处；对象不存在 = 高危（引用悬空的根源），对象存在但内容不同 = 中危；
4. **未识别命令指纹对比**：对解析器不认识的命令（qppb-policy 52、carrier 64、trust 51、mpls 176 条），不做语义理解，按命令类型把行规范化成指纹集合，对比"老设备 N 条 → 新设备 M 条，缺失 K 条"，直接回答"这些策略迁没迁全"。纯确定性、零成本，把 700+ 条漏网行纳入核查而非晾置。

输出：**资产级迁移报告**（端口对 / 系统项 / 对象 / 命令指纹四类，每类列出 缺失 / 差异 / 一致）。

### L3 协议核查 + 状态验证

**配置层（确定性）**：BGP/ISIS/LDP 邻居按对端 IP 配对，核查数量与关键参数（AS、Hold、认证、地址族、策略引用）是否对齐；协议进程/区域/locator 逐项对比。本样本验收基准：BGP 6→6 应全部配对。

**状态层（自动验证）**：由 L1-L2 结论 + 协议配对生成**验证清单**（如"9 个 BGP 邻居应全部 Established"、"MTU 9500 的端口实际生效"），复用现有 SSH 采集能力自动上设备执行对应 display 命令，实际输出与预期对照，输出"验证通过 / 失败 / 待确认"。

### L4 引用完整性核查（确定性初筛 + LLM 兜底）

**前提（本样本教训）**：必须先建立覆盖全格式的全局对象定义库，包括 `acl number N` / `acl N` / `acl name X` / `acl ipv6 name X`、`ip ip-prefix X` / `ip ipv6-prefix X`、`route-policy X`、`ip community-filter X` / `community-filter X`、`ip vpn-instance X`，排除 `undo` 行、允许缩进。格式覆盖不全必然误报（本样本初测 106/75 个"悬空"复核后绝大多数为误报）。

引用来源**不限于端口**：端口配置、协议配置（BGP/路由策略）、QoS 策略中的引用都要核查。流程：提取全部引用 → 反向匹配定义库 → 确定性判定"引用无定义"；格式模糊的交 LLM 判断（给足上下文窗口）；悬空按严重级输出（高危：路由/策略失效；中危：过滤不生效；低危：命名引用待确认）。
## 5. 输出报告形态

割接对比结果页新增"割接迁移核查"面板，自上而下：

1. **迁移完整性总览**：四类资产（端口 / 全局系统 / 全局对象 / 命令指纹）的 一致 / 差异 / 缺失 / 新增 计数卡片；
2. **疑似漏迁清单**（置顶，按影响排序）：before 有、after 无配对的老端口与全局对象；每条可展开看原始配置；
3. **迁移对明细**：每对展开显示逐项对比结果（丢/改/增 + 原始行）；
4. **全局对象对比结果**：对象缺失（高危）/ 内容差异（中危）/ 一致，含定义行 diff；
5. **未识别命令指纹结果**：qppb-policy / carrier / trust / mpls 等各"迁了 N/M 条、缺失 K 条"，缺失行直接列出；
6. **协议核查结果**：邻居配对表 + 状态验证结论（含自动 SSH 采集的实际输出）；
7. **引用完整性**：悬空引用清单（含引用位置与建议）；
8. **解析覆盖率提示**：漏网行类型与数量，标注对结论置信度的影响。

每条结论带"置信度/证据/人工确认"标记；LLM 生成的内容单独标识"AI 建议，仅供参考"。

---

## 6. LLM 职责边界（收窄）

| 职责 | 输入 | 输出 | 是否必须 |
|---|---|---|---|
| 空端口模糊匹配（无 IP 无描述） | 两侧空端口配置块 | 候选迁移对 | 可选 |
| 引用悬空语义判断 | 引用行 + 定义库候选 | 判定/建议 | 可选 |
| 差异风险解释 | L2 差异项 | 风险说明 + 建议 | 可选 |
| 验证清单 / 回滚预案生成 | L1-L3 结论 | 验证命令序列 | 推荐 |

任何 LLM 输出不得直接修改配置或自动入库；关键结论必须人工确认。

---

## 7. 实施里程碑

| 阶段 | 内容 | 验收基准（本样本） | 风险 |
|---|---|---|---|
| M1 | L1 端口映射 + L2 资产逐项对比（端口 / 全局系统 / 未识别命令指纹，纯确定性） | 54 IP 对 + 13 描述对全部正确；Eth-Trunk6 检出 MTU 差异；40 空端口正确标注；qppb-policy/carrier/trust/mpls 指纹对比输出缺失清单 | 低 |
| M2 | L3 协议核查（配置层配对 + 验证清单 + 自动 SSH 状态对照） | BGP 6→6 全部配对；验证清单可执行 | 低 |
| M3 | 全局对象逐项对比 + L4 引用完整性（全格式定义库） | 对象定义 58+ 全对比；0 格式误报；悬空清单人工复核 | 中 |
| M4 | 报告 UI（迁移核查面板）+ 导出 | 面板展示 6 块内容 | 低 |
| M5 | 多设备协同（邻居双向核查）+ 历史对比 | 两台设备 BGP 双向一致 | 中 |

**建议顺序**：M1 → M2 → M4（先让用户看到真实迁移报告）→ M3 → M5。

---

## 8. 与旧设计的关系

- 替代 `semantic-diff-design-v1/v2`：放弃"双通道单次调用 + 反哺解析器"骨架；
- 保留的底线：确定性 diff 权威、人工确认、脱敏、失败降级、报告可溯源；
- 保留的组件：`collectUnknownLines`（降级为覆盖率提示）、`runComparePure`（作为 L2/L3 的对比基础）、SSH 采集（作为 L3 状态验证）、LLM 代理（作为兜底）。

> 一句话：**确定性引擎做"搬没搬全"（权威），LLM 做"说不清的帮你判断"（兜底），自动 SSH 做"活没活"（验证）**。
