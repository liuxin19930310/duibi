# 补充建议：横向维度篇（前两篇未覆盖的方向）

> 前两篇：`optimization-proposals.md`（功能清单）+ `optimization-deep-dive.md`（架构/算法纵深）。
> 本篇补 **6 个横向维度**——它们横跨功能与架构，是让工具从"个人利器"变成"团队/生产级平台"的关键，且前两篇未充分展开。

---

## 1. 性能与规模（现网大数据量工程）
- **现状**：接口信息 16 列、万级接口/百台设备场景未验证；`deviceParser.js` 解析在主线程，表格无虚拟滚动。
- **做法**：解析卸载到 **Web Worker**（避免大 config 卡死 UI）；el-table / 原生 table 启用**虚拟滚动**（百万行）；**流式解析**（大 output 边收边解，契合后端 SSE）；IndexedDB **分片存储**历史快照。
- **价值**：从"小场景可用"到"现网规模可用"——割接工具不上规模就没法进生产。
- **难度**：中。

## 2. 集成生态（嵌入现有运维体系，不做孤岛）
- **对接对象**：CMDB / NetBox（设备与拓扑的来源，避免手工录入）、ITSM / 工单系统（割接工单流转）、监控 Zabbix / Prometheus（割接时拉实时指标佐证成功）、Git（配置版本化 + diff 历史）、钉钉 / 飞书（审批与告警推送）。
- **价值**：割接数据自动流转，减少手工搬运与信息孤岛。
- **难度**：中（取决于对方 API 成熟度）。

## 3. 协作与团队工作流
- **现状**：纯单机本地（IndexedDB），无共享、无评审。
- **做法**：割接**工单 + 评审**（多人批注对比结果）、**操作留痕共享**、**角色分工**（架构师配方案 / 执行人采设备 / 审核人签字）、**变更窗口管理**。
- **价值**：从个人工具 → 团队协作；满足割接"双人复核"的合规硬要求。
- **难度**：高（需服务端 + 账号体系，与 deep-dive 3.3 鉴权联动）。

## 4. 安全纵深（生产环境底线）
- **现状**：无鉴权，SSH 凭据（密码/密钥）在前端/浏览器存储。
- **做法**：凭据进**服务端 vault**（绝不落前端）；跳板/代理 SSH；**高危命令拦截**（write/commit 二次确认）；操作二次认证；敏感配置**脱敏展示** + 导出加密。
- **价值**：进了生产环境，这是不可妥协的底线，否则工具本身成攻击面。
- **难度**：中高。

## 5. 数据资产沉淀（让系统越用越聪明）
- **做法**：设备/拓扑 **CMDB**（自动从采集沉淀）、**厂商版本适配矩阵**（华为 VRP 各版本解析差异记录，解析失败可溯源）、**历史问题/案例库**（解析失败自动聚类 → 知识库）、**配置模板库**（复用割接模板）。
- **价值**：知识不随人走；每次割接都反哺下一刀。
- **难度**：中。

## 6. 可视化洞察层（不止表格）
- **做法**：割接**风险评分仪表盘**、**配置漂移热力图**（哪些设备/接口偏离基线）、**光功率/错误率趋势图**、**变更前后链路通断模拟动画**、**端口占用矩阵**。
- **价值**：从"看数据"到"看结论"，管理层与执行层各取所需。
- **难度**：中（需可视化库，ECharts 已可复用）。

## 7. 智能辅助的细分落地（与 LLM 互补，偏数据驱动）
- **解析自纠错**：解析失败的配置自动聚类，区分"设备版本差异"还是"解析器 bug"。
- **异常配置自动分类打标**（端口 err-disable、单边邻居、黑洞路由）。
- **基于历史成功割接案例，推荐本次方案模板**。
- 这类不依赖大模型，纯规则/数据驱动，可先于 2.5 LLM 落地。

---

## 三篇合起来就是完整蓝图
| 文档 | 视角 | 回答的问题 |
|---|---|---|
| `optimization-proposals.md` | 功能清单 | 加哪些功能 |
| `optimization-deep-dive.md` | 架构/算法纵深 | 建什么底座、用哪些算法范式 |
| `optimization-supplement.md` | 横向平台化维度 | 如何上规模、进团队、进生产 |

三者**正交**：proposals 是"做什么"，deep-dive 是"怎么做得深"，supplement 是"怎么做得广"。落地的稳妥顺序是：先 deep-dive 的 P0 地基，再 proposals 的高频功能，过程中用 supplement 的规模/安全/协作标准约束设计，避免返工。
