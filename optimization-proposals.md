# NetCompare 系统优化与功能新增建议

> 基于代码现状（netops-pro 2.0.0）的分析。架构：前端 Vue3 + Element Plus + Vite（路由懒加载、keep-alive tab）；后端 Node + ssh2 真实 SSH 采集（华为/华三，config/status/full，常驻会话 + SSE 实时同步，连通性测试，health）；持久化走 IndexedDB（db.js）。解析器矩阵完整：bgp / isis / ldp / ldpPeer / srv6Sid / srv6TePolicy / routingStat / interfaceInfo / ospf / lldp / arp / ipv6neigh。
>
> 分三档：技术债（有踩坑风险）、体验补全（低风险高回报）、新增功能（按价值排序）。

---

## 一、必须先处理的技术债

1. **采集后端无任何鉴权**
   `/api/collect`、`/api/device/connect`、`/api/test-connection` 任何人可调用，且能 SSH 任意目标设备。作为纯本地内部工具可接受，但必须：①文档化风险，禁止把后端暴露到公网；②至少加 origin 校验或限制仅 `127.0.0.1` 回环访问；③`/api/device/connect` 把明文密码/privateKey 存入内存 `sessions` Map，进程存活期间驻留，建议在连接释放时主动擦除。

2. **SSH 采集靠"静默检测"判定结束**
   `runShellCollect` 用 `idleMs` 轮询静默来收尾。慢设备、大配置（status/full 数据量大）极易被误判结束导致截断，或反之挂等到硬超时。建议改用"命令结束锚点"：等待设备回显下一行提示符（如 `<Huawei>` / `[H3C]`）作为该命令结束信号，而非纯静默。

3. **首包体积无分包**
   构建有 chunk 体积警告。解析器多且大（十几个 .js）+ Element Plus，建议 `build.rollupOptions.output.manualChunks` 拆 vendor / element-plus / 解析器三块，首屏只拉首页所需。

4. **Element Plus 引入方式未确认按需**
   若用 `unplugin-auto-import` + `unplugin-vue-components` 则为按需，否则全量引入拖慢首屏。先确认，不是按需就改按需。

5. **导出注册逻辑需防回归**
   `useExportAll` 按 page 注册/注销，keep-alive 切页下易残留旧 fn 导致误导出。之前已修过一次，建议补一个最小 E2E（切到对比页→导出→切走→再切回）防回归。

---

## 二、现有功能的体验与能力补全（低风险高回报）

1. **接口信息表格 16 列 → 列显隐选择器 + 默认核心列**（最高优先级体验项，仍未做）
   当前 `InterfaceInfoModule.vue` 14 个叶子列 + 物理层/配置/质量分组，横向很宽。加一个 el-dropdown 列显隐，默认只显示接口/状态/结果/IP/光功率等核心列。

2. **ProtoPanel 头部过载**
   标题 + 描述 + 总数 + 4 个 badge + 5 个 filter tab + 搜索 + 导出挤在一行。建议把 5 个 filter tab 收到下拉，或拆成两行（标题行 / 工具栏行）。

3. **配置对比"只接受文本" → 直连设备采集**
   既有后端能 SSH 采集，对比页却只能手动贴"变更前/后"两份配置文本。应支持"连设备采 A / 连设备采 B"，免手动导出粘贴，体验跃迁最明显。

4. **实时同步（在线设备）缺"变化高亮"**
   SSE 已推送原始状态，前端也应保存上一轮，本轮 vs 上轮 diff：变化项（BGP 邻居 down、路由数波动、SRv6 policy 消失）红/绿高亮。割接验证最关心的就是这个。

5. **快照页缺"快照对比"**
   已能存配置快照（snapshots.js + IndexedDB），但只能单看。加"选两个快照 diff"（类 git diff）是天然延伸，价值高、成本低。

6. **文本逐行对比增强**
   `TextDiffPage` 已有，建议加"忽略空白 / 忽略时间戳行 / 正则过滤行"选项，运维配置 diff 经常要忽略这些噪音。

7. **缺全局搜索**
   各 ProtoPanel 内有搜索，但没有跨所有协议模块的全局搜索。顶部加一个"搜接口/IP/VRF，跨 BGP/ISIS/接口信息…"的全局入口。

---

## 三、值得新增的功能（按价值排序）

1. **割接任务编排（Cutover Task）** — 价值最高
   把"设备组 + 采集计划 + 前后基线 + 验证项清单"组织成一个任务，首页看整体进度与通过率。当前"割接汇总"页只展示结果，缺编排层。

2. **配置合规基线检查** — 成本低（复用已有解析器）
   用解析器输出定义规则（如"上行口 description 必填""BGP 必须配最大路由限制""互联口禁止 shutdown"），对设备配置做合规扫描，输出不合规项清单。

3. **变更影响预评估** — 比纯 diff 有用
   导入拟变更配置，解析出"本次变更会动到哪些 BGP 邻居、哪些 SRv6 policy、哪些接口"，而不是只给文本差异。

4. **设备拓扑图（基于 LLDP）** — 中等工作量、惊艳
   已有 `lldp.js` 解析器，画力导向邻居拓扑，链路/设备一眼看全。割接前理清上下游关系很实用。

5. **多设备批量采集** — 后端加并发即可
   设备清单 CSV → 并发 SSH 采集 → 汇总成基线库。割接前批量收基线最刚需。

6. **对比历史记录** — 低成本
   `db.js` 已持久化，加一个"对比历史"列表页，每次对比留痕（时间/设备/差异数），可回溯。

7. **导出 PDF 报告** — 增值
   当前仅 Excel。加"割接验证报告"PDF 模板（设备、采集时间、各项通过/不通过、差异摘要）。

8. **离线 CLI** — 复用后端
   后端已独立，`netops collect <host>` 包装成命令行，脱离前端做批量/自动化采集。

---

## 优先级结论

- **第一波（立刻变好、成本低）**：接口信息列选择器、配置对比直连设备采集、快照对比、全局搜索、首包分包。
- **第二波（差异化能力）**：割接任务编排、实时变化高亮、合规基线检查、变更影响预评估。
- **第三波（增值亮点）**：设备拓扑图、批量采集、PDF 报告、对比历史、CLI。

> 注：UI/UX 审计报告（ui-ux-audit.md）里的视觉类建议（a11y、深色表格边框、浅色 --t3 对比度、H3C 背景混淆、收起截 bug）已于 2026-08-04/05 陆续修复，不在本清单重复。
