# 深度优化与能力升级路线图（进阶篇）

> 本文是 `optimization-proposals.md` 的进阶篇。前者回答"加哪些功能"，本文回答"建什么能力底座、用哪些算法/架构范式"——从功能清单升级到体系能力，是和同类工具拉开代差的方向。
>
> 现状依据（已核实真实代码）：
> - 采集：`server/index.js` 是真实 Node+ssh2 服务（config/status/full、常驻会话+SSE），但采集层→解析层是**一次性管道**，无中间领域模型。
> - 解析：`src/utils/deviceParser.js` 命令式解析器矩阵，输出扁平对象数组，**无统一 schema**，UI/对比/导出三套字段口径靠各自定义。
> - 状态：`src/store/`（auth/tabs）+ `src/utils/scope.js` 的 composables 全局 ref，跨页靠手动注册（如 `useExportAll`）。
> - 类型：纯 JS，无 `.d.ts`，前后端无契约。
> - 持久化：IndexedDB（`db.js`），无版本迁移。

---

## 1. 架构底座层（最深的杠杆）

### 1.1 统一领域模型 + 协议 Schema 驱动
- **问题**：每个协议解析器自己决定输出结构，导致导出/对比/解析三套字段口径不一（之前多 sheet 导出本质是用构建函数 workaround 这种不一致）。换厂商或加协议，UI/导出/对比要跟着改。
- **做法**：定义「网络设备配置领域模型」——用 JSON Schema / 轻 DSL 描述每种协议（bgp/isis/ldp/srv6/interface/...）的实体、属性、关系。解析器只做"文本 → 领域模型实例"，**UI、对比、导出、拓扑全部从领域模型派生**。
- **价值**：一份 schema 驱动比对、导出、校验、可视化；新增厂商/协议**零 UI 改动**；可做 schema 版本迁移。这是后面所有智能能力的前提。
- **难度**：中高（需重构 `deviceParser.js` 出口）。

### 1.2 可插拔解析引擎（Plugin Architecture）
- **做法**：把 `deviceParser.js` 重构成「解析器注册表 + 适配接口」，厂商（华为/H3C/未来 Cisco/Juniper）、协议都以插件注册；配合 1.1 的 schema，"新增厂商 = 加一个 parser 插件 + 一份 schema"。
- **价值**：可维护性质变，且可开放给用户自定义解析规则。
- **难度**：中。

### 1.3 领域事件总线（取代"手动注册"的拉模式）
- **问题**：跨页共享靠 `scope.js` 的全局 ref，页面激活时才注册导出函数（`useExportAll`），本质是拉模式、易竞态（之前多页竞态 bug 即源于此）。
- **做法**：引入轻量领域事件总线（设备采集完成 / 配置变更 / 对比结果就绪），组件订阅事件而非轮询或手动注册。
- **价值**：解耦采集—解析—展示，为「实时变化高亮」「跨模块全局搜索」提供骨架。
- **难度**：低中。

---

## 2. 算法与智能层（技术含量最高）

### 2.1 语义级配置 Diff 引擎（取代行级/字段级）
- **问题**：当前比对是字段值相等（`compareCore.js`），不懂"语义等价"——ACL 顺序不同但等价、BGP 邻居描述换位、路由策略逻辑等价都会被报成"差异"。
- **做法**：在领域模型上做**语义 diff**：归一化（顺序无关集合、通配符展开、逻辑表达式化简）→ 计算结构差异 → 输出"语义变更"而非"文本变更"。
- **价值**：割接对比从"看差异"升级到"懂变更"，误报告警大幅下降。
- **难度**：高。

### 2.2 配置依赖图 + 变更影响传播（图算法）
- **现状**：`lldp.js` 已能解析邻居，但没构建全局依赖图。
- **做法**：基于解析出的接口/邻居/BGP 邻居/Policy 引用，构建**设备内 + 跨设备依赖图**；变更某接口/策略时，用图遍历（BFS/DFS + 影响权重）预测受影响范围（下游邻居、路由、业务）。
- **价值**：变更影响预评估从拍脑袋变成可计算；配合 1.1 schema 还能校验引用完整性（悬空引用=配置错误，提前发现）。
- **难度**：中（图构建）+ 高（影响权重模型）。

### 2.3 可解释合规基线（策略即代码，非正则堆砌）
- **做法**：引入**策略即代码**（Rego 或自研轻 DSL），把"禁止弱密码""BGP 须配 MD5""互联接口 description 必填"写成策略；引擎评估并生成**可解释报告**（哪条规则、哪台设备、哪行配置、建议修复）。
- **价值**：合规可审计、可版本化、可评审；复用已有解析器，成本低。
- **难度**：中。

### 2.4 异常检测与基线偏离（时间序列）
- **现状**：快照是时间点存储，无纵向比较。
- **做法**：对历史快照做**时间序列基线**（接口错误率、BGP session 数、CPU 等），新快照偏离基线 N 个标准差即告警，并解释"偏离什么、偏离多少"。
- **价值**：从"对比两个状态"升级到"持续监控偏离"，贴近 AIOps。
- **难度**：中。

### 2.5 大模型（LLM）集成（最高杠杆的人机交互）
- 基于结构化领域模型，做：自然语言查询（"哪台设备 BGP 邻居没起来？"→ 转领域模型查询）、自动生成割接方案/变更脚本草案、自动撰写复盘与合规说明、配置审计问答。
- **价值**：把专家经验产品化。
- **注意**：数据安全——本地模型或脱敏网关，避免把设备配置外发。
- **难度**：中（集成）+ 取决于模型策略。

---

## 3. 工程化与可靠性（让系统可信赖）

### 3.1 前后端契约与类型安全
- 纯 JS 无 `.d.ts`，字段靠约定。建议用 OpenAPI 描述 `/api/collect` 等接口，前端生成 TS 类型；解析器输出用 TS interface 约束。
- **价值**：字段错位类 bug（之前 `ref` 未 unwrap 那一类的近亲）在**编译期**暴露。
- **难度**：中。

### 3.2 端到端测试与快照回放框架
- 采集到的真实设备输出是稀缺测试资产。把脱敏后的采集样本存为 fixture，建"解析器回放测试"（输入样本 → 断言领域模型），防止解析器回归。
- **价值**：解析器是系统核心资产，必须有回归保护。
- **难度**：低中。

### 3.3 采集健壮性与权限审计
- 深化之前的"SSH 静默收尾易截断"：采集层埋点（耗时/字节/截断检测/重试分级）、SSE 心跳与断线重连。
- 把"无鉴权"深化为 **RBAC + 操作审计日志**（谁、何时、对哪台设备、做了什么）——这是从玩具走向生产工具的硬门槛。
- **难度**：中。

### 3.4 数据迁移与离线优先
- IndexedDB 无版本迁移策略。建议加 migration + 工作区导出/导入（整个割接项目打包带走，含快照/配置/结论）。
- **难度**：低中。

---

## 4. 产品闭环（从工具到平台）

### 4.1 割接全生命周期闭环
- 现状：割接汇总只展示结果。建议打通：设计（拓扑/方案）→ 预审（合规+影响评估）→ 执行（直连采集验证）→ 验证（快照对比）→ 回滚（一键回退脚本）→ 复盘（LLM 报告）。
- **价值**：从"对比工具"变成"割接作战平台"。
- **难度**：高（跨模块串联）。

### 4.2 配置仿真 / 数字孪生
- 在沙箱或只读采集+规则模拟中跑拟变更配置，预测结果而**无需真改设备**。
- **价值**：变更零风险预演。
- **难度**：高。

### 4.3 业务 / SLA 关联视图
- 把"配置对象"映射到"业务/链路/SLA"，割接影响直接显示"影响哪些业务、SLA 余量"。
- **难度**：中（需业务拓扑数据输入）。

---

## 5. 落地优先级（深度版）

| 优先级 | 项 | 为什么 |
|---|---|---|
| **P0 底座** | 1.1 领域模型 + 1.2 插件化解析 | 一切智能能力的前提，越早做越好 |
| **P1 智能** | 2.1 语义 diff、2.2 依赖图/影响传播 | 直接提升核心对比体验，区分度最高 |
| **P2 可靠** | 3.1 类型契约、3.2 回放测试、3.3 鉴权审计 | 从可用到可信赖 |
| **P3 产品** | 4.1 全生命周期、2.5 LLM、4.2 仿真 | 平台化跃迁 |

> 一句话：上一版是"加功能让你更快"，这一版是"建底座 + 上算法让你和同类工具拉开代差"。建议从 **P0 的 1.1/1.2** 起步——它不炫，但决定了后面所有能力是"搭在沙子上"还是"搭在地基上"。

---

## 6. 与上一版的关系
- 上一版 `optimization-proposals.md` 的功能清单（接口信息列选择器、配置对比直连采集、快照对比、全局搜索、割接编排、合规基线、拓扑、批量采集等）**仍然成立**，本篇是它们的"下层支撑"和"高阶形态"。
- 例如：上一版的"合规基线"在本文升级为 2.3「策略即代码 + 可解释报告」；"快照对比"在本文升级为 2.4「时间序列基线偏离」；"LLDP 拓扑"在本文升级为 2.2「依赖图 + 影响传播」。
